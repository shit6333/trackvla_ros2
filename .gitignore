/build/
/install/
/log/
/cache/
/.env
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# Scratch scripts copied into the workspace so the container's bind mount can
# see them. One escaped into a commit once; this keeps it from recurring.
/_*
