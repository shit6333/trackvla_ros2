<?xml version="1.0"?>
<model>
  <name>test_robot</name>
  <version>1.0</version>
  <sdf version="1.9">model.sdf</sdf>
  <description>
    Minimal differential-drive robot for validating the ROS pipeline: a front
    camera that feeds the model, a chase camera that records what happened,
    and nothing else.
  </description>
</model>
